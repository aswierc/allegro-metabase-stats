"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getExpiresInHours = void 0;
exports.getExpiresInHours = function (tokens) {
    var now = +new Date();
    if (!tokens || !tokens.created_at)
        return 0;
    var expiration = new Date((tokens.expires_in + tokens.created_at) * 1000);
    var expHours = (+expiration - +now) / 1000 / 60 / 60;
    return expHours;
};
//# sourceMappingURL=helpers.js.map